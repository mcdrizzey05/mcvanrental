<div class="container">
  <div class="row">
    <div class="col-9"><%= @offer.title %></div>
    <div class="col-4"><%= image_tag (@offer.img_url), class: "card-img-top"%></div>
      <div class="col-6">
        <%= @offer.description %>
        <p>Price: €<%= @offer.price %>/night</p>
      </div>
  </div>
</div>
